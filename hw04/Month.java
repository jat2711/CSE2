////////////////////
// Joanna Targowski
// Month Java Program
// My program will ask the user to enter an integer for the month.
// Then the program will display the number of days in the month.
//
//  first compile the program 
//      javac Month.java
//  run the program 
//      java Month

import java.util.Scanner;

//define  class
public class Month{
    
//  add main method 
        public static void main(String[]) { 
            
        Scanner myScanner;
        myScanner=new Scanner (System.in)
        
        System.out.println("Enter an int giving the number of the month (1-12) ");   // Check if the integer is entered for the month and it is in the required range.
        int nMonth=myScanner.nextInt();
        if (myScanner.hasNextInt())
        
        else {
                System.out.println("Are you sure the amount entered is an integer and is in the range specified?");
        }
        switch (Month) {
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
        
        if (MonthNumber==2) {
                System.out.println("Please enter the year");
                if ((Year % 4 == 0) && (Year % 400 == 0) && ! (Year % 100 == 0))
        }
        
        if month
                
        }
        
        
        
        
        

} // end of main method 
} // end of class